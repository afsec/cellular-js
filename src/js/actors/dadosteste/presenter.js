const actorDadosTestePresenter = () => {
    debug(`actorDadosTestePresenter()`)
    const model = actorDadosTesteModel()
    const view = actorDadosTesteView(model)
    // AddEvenListener, innerHTML or Call Another State.
}

